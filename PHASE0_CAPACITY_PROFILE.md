# 阶段 0：V1 容量与 SLO 基线

## 状态

本文件是阶段 0 的工程基线，不是生产规模承诺。域名保持占位符，实际生产域名和容量扩容仅能通过后续变更记录调整。本基线用于建库、索引、Worker 数量、压测参数和告警阈值，禁止以“数据大了再改”替代。

## 冻结输入

| 变量 | 值 | 说明 |
| --- | ---: | --- |
| 峰值活跃付费用户 | 1,000 | 同时有至少一个启用任务的用户 |
| 每用户峰值采集器 | 2 | 同账号设备绑定上限 |
| 每用户启用搜索任务 | 20 | 不含竞品商家任务 |
| 每搜索任务每日扫描 | 12 | 两小时一次的容量基线 |
| 单搜索扫描商品数 P50 / P95 | 30 / 60 | 以真实多页采集上线后重新校准 |
| 每用户竞品卖家 | 5 | 独立卖家监控上限 |
| 每卖家每日抓取页数 | 4 | 卖家商品页基线 |
| 商品或卖家变化率 | 3% | 仅变化状态进入云端版本/事件链 |
| 每变化版本新媒体数 | 0.08 | 媒体按 SHA-256 去重 |
| 新媒体 P95 大小 | 750 KB | 原图原始对象上限基线 |
| 热数据保留 | 6 个月 | PostgreSQL 可直接查询 |
| 归档保留 | 18 个月 | 压缩对象归档，可恢复 |
| 审计保留 | 24 个月 | 仅允许追加，按月分区管理 |
| 原始公开快照保留 | 90 天 | 仅公开页面原始对象 |
| 图片保留 | 18 个月 | 引用计数为 0 且超过窗口后删除 |
| 峰值因子 | 10 | 用于上传与队列峰值 |
| 每用户每日 AI 调用 | 10 | 由套餐权益继续限制 |
| 备份 RPO / RTO | 15 分钟 / 4 小时 | 阶段 1 必须演练 |

## 派生容量

```text
daily_search_runs = 1,000 * 20 * 12 = 240,000
daily_search_observations_p95 = 240,000 * 60 = 14,400,000
daily_seller_observations_p95 = 1,000 * 5 * 4 * 60 = 1,200,000
daily_parsed_observations_p95 = 15,600,000
daily_changed_records_p95 = 15,600,000 * 3% = 468,000
peak_changed_record_ingest_rps = ceil(468,000 / 86,400 * 10) = 55
monthly_new_media_storage_p95 ~= 785 GiB before cross-item media deduplication
```

采集器以本地基线过滤未变化记录：云端接收变化版本、事件、采集运行摘要和需要审计的拒绝记录，而不是把每次搜索的每个未变化商品都写入热事实表。原始可共享快照通过对象存储生命周期控制。

## SLO 与压测门禁

| 场景 | 目标 |
| --- | --- |
| User/Admin 列表首页与下一页 | P95 <= 300 ms，P99 <= 800 ms，错误率 < 0.1% |
| 商品、卖家、事件、动态日志深分页 | 100,000 条基线内连续分页无重复、无漏项；P95 <= 500 ms |
| 采集批次接收 | 持续 55 changed-record/s，P95 <= 750 ms，P99 <= 2 s，幂等重复写入为 0 |
| 队列积压恢复 | 峰值 30 分钟积压在 60 分钟内清空 |
| AI | 95% 任务在 5 分钟内开始；同一幂等键只扣费一次 |
| 登录与设备令牌刷新 | P95 <= 300 ms，P99 <= 800 ms，错误率 < 0.1% |
| 可恢复性 | 按 RPO 15 分钟、RTO 4 小时完成演练 |

## 域名占位

| 用途 | 占位地址 |
| --- | --- |
| User Web | `https://user.placeholder.invalid` |
| User API | `https://user-api.placeholder.invalid` |
| Collector API | `https://collector-api.placeholder.invalid` |
| Admin Web | `https://admin.placeholder.invalid` |
| Admin API | `https://admin-api.placeholder.invalid` |

`.invalid` 是 RFC 保留后缀，只允许用于本阶段文档和测试配置；不得作为可访问生产地址。
