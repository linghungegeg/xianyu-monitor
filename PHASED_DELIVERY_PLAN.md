# 闲鱼市场监控：分阶段交付方案

## 1. 产品边界

本项目由三个独立、可单独构建和部署的产品组成：

- **用户 Web / 未来用户 APK**：用户登录、监控任务、竞品商家、市场商品池、市场发现、事件中心、动态日志和 AI 结果。
- **采集启动器**：Windows 轻量程序。用户用本系统账号登录并绑定设备，打开本机专用 Chrome 登录闲鱼，执行采集和静默上传。
- **Admin Web**：独立域名、独立登录、独立构建和部署，用于运营、权限、计费、数据、AI、审计和系统容量管理。

用户产品、采集启动器和 Admin 不复用界面、发布包或部署单元。它们只通过本项目自有的加密 API 协作。

### 1.1 明确约束

- 闲鱼 Cookie、Token、扫码登录态、浏览器 Profile 和闲鱼账号操作状态只保存在采集启动器所在机器。
- 采集启动器登录的是本系统的用户账号和密码，不是把闲鱼账号密码上传云端。
- 云端不登录闲鱼。云端接收约定的公开商品、公开卖家、图片、快照和事件数据。
- 用户 Web 不提供 AI 模型、Key、提示词、阈值和队列设置；用户只调用已发布的分析能力并查看结果。
- 用户 Web 不显示上传入口、上传数量或同步状态。采集启动器使用本地 Outbox 静默补传；上传批次、失败和重试详情仅在 Admin 可见。
- 用户账号和 Admin 账号是独立认证域。Admin 账户由后台单独创建和授权，不能使用普通用户入口获得管理权限。

### 1.2 用户侧视觉与交互基线

用户 Web 与 Admin Web 采用懒人铺货的工作台布局语言：固定可收起侧栏、顶部状态栏、紧凑筛选与数据表、详情抽屉、指标卡片和趋势图。仅参考视觉和交互密度，不复用任何代码、接口、数据库或部署配置。

用户 Web 导航：

```text
Dashboard
我的监控
竞品商家
市场商品池
市场发现
事件中心
动态日志
AI 分析
设置
```

Admin Web 导航：

```text
运营概览
用户与设备
套餐、订单与用量
市场商品与卖家
类目与数据质量
上传批次与事件
AI 配置与任务
队列、存储与容量
审计与系统设置
```

### 1.3 全局分页规则

所有可增长列表必须服务端分页，禁止一次拉取全量：监控任务、竞品商家、商品、市场发现、事件、动态日志及所有 Admin 表格。

统一使用 `limit + cursor + sort + filters` 查询契约：默认 50 条，可选 20 / 50 / 100 条；服务端返回 `nextCursor`、`hasMore` 和总量。排序必须附带稳定唯一 ID 作为次级排序，筛选或排序变化时从第一页重新查询，避免翻页漏项和重复。详情、图片和历史按需加载。

## 2. 阶段门禁

每个阶段固定执行：实现 -> 定向测试 -> 独立复检 -> 记录验收证据 -> 留稳定点。未完成本阶段的验收项，不进入下一阶段。除阶段 8 外不打正式安装包或 APK。

## 阶段 0：架构、数据和容量合同

### 做什么

- 定义 User Web、Admin Web、采集启动器、User API、Admin API、采集器 API 的边界和独立部署方式。
- 设计 PostgreSQL、对象存储、Redis、消息队列和搜索索引的职责；数据库从第一天按分区、索引、幂等和生命周期设计。
- 定义用户、Admin、设备、套餐、订单、用量账本、商品、卖家、类目、快照、事件、上传批次、AI 任务和审计数据模型。
- 冻结并发用户数、每用户任务数、采集间隔、日快照量、历史保留期、图片存储量和查询 SLO，作为容量合同。

### 验收

- 全部实体字段、主键、去重键、索引、分区策略、保留策略和访问边界可追溯。
- 迁移可重复执行；关键商品、事件、卖家和日志查询有索引计划。
- 容量合同明确写入，不以“数据大了再改”为前提。

## 阶段 1：独立云端基础、账号与扣费

### 做什么

- 建立独立的 User API、Admin API、用户账号域、Admin 账号域、设备绑定和短期令牌机制。
- 建立套餐、订单、订阅、用量账本、权益策略和扣费流水；云端是唯一权威。
- 部署 TLS、令牌轮换、Argon2id 密码哈希、加密备份、对象存储加密、审计、限流和告警基础。

### 验收

- 用户、Admin、采集设备三种身份无法越权访问彼此接口。
- 套餐或余额变化可以改变采集器的任务数、频率和 AI 权限；本地修改不能伪造权益。
- 密码、令牌、对象和备份均不明文；服务重启、令牌过期和设备解绑可恢复。

## 阶段 2：用户 Web 与 Admin Web 工作台

### 做什么

- 分别建设 User Web 与 Admin Web 的独立入口、侧栏、顶部状态、筛选栏、数据表、详情抽屉和分页组件。
- 用户 Web 先完成 Dashboard、我的监控、竞品商家、市场商品池、市场发现、事件中心、动态日志、AI 分析和设置页面壳。
- Admin Web 先完成运营概览、用户与设备、套餐订单用量、市场数据、AI、队列存储和审计页面壳。
- 动态日志记录任务、采集、规则变更、浏览器状态、本地异常与 AI 调用结果；不展示静默上传明细。

### 验收

- 用户与 Admin 均使用独立登录、独立构建产物和独立部署配置。
- 每个可增长列表均使用全局分页契约，筛选、排序、空态、加载态、失败重试和详情抽屉可用。
- 用户侧不存在 Admin 菜单、AI 配置、模型 Key、提示词编辑或上传状态页面。

## 阶段 3：轻量采集启动器与设备绑定

### 做什么

- 将现有 Electron 原型收敛为采集启动器，只保留本系统账号密码登录、设备绑定、Chrome 登录、启动/暂停采集、最小日志和异常提示。
- 保留本机 SQLite、专用 Chrome Profile、任务调度和本地 Outbox；移除桌面工作台、商品池、竞品、AI 和上传页面职责。
- 使用云端签发的短期采集器令牌和权益策略，按设备和套餐执行任务。

### 验收

- 同一用户在新设备登录需绑定，解绑后旧设备不能继续采集。
- 启动器中不保存本系统明文密码，也不上传闲鱼 Cookie、Token 或 Profile。
- 启动器可在没有云端页面的情况下完成本地 Chrome 扫码和授权采集；正常用户无需看到上传过程。

## 阶段 4：搜索、类目和商品采集

### 做什么

- 解析闲鱼一级、二级、三级类目，以及关键词、综合/新降价/新发布/价格排序、价格区间、区域和公开筛选条件。
- 实现真实翻页、商品详情解析、本地基线去重和商品版本快照。
- 任务规则支持关键词、类目、价格、地区、页面筛选、包含词、排除词和采集频率。

### 验收

- 对一个真实关键词和类目连续采集多页，稳定取得商品 ID、标题、价格、区域、图片、标签、发布时间、想要数和可见详情。
- 同条件再次采集不重复插入商品或快照；筛选条件与闲鱼页面实际条件一致。
- 登录失效、Chrome 不可用、页面结构异常和网络错误都有可操作的本地日志。

## 阶段 5：竞品商家监控

### 做什么

- 支持从商品详情一键添加卖家或粘贴公开卖家主页。
- 解析卖家公开资料、公开在售/已售商品与商品详情；建立卖家、商品、快照和事件关系。
- 识别卖家上新、降价、涨价、下架、已售、重新上架、标题/描述/图片/成色变化，并支持独立筛选和分页。

### 验收

- 对一个真实公开卖家完成在售和已售商品的多页采集。
- 无变化的二次扫描不新增事件；使用两份真实结构快照验证各类变化事件生成正确。
- 卖家商品、事件和详情页面的分页、筛选和时间线均不重复、不漏项。

## 阶段 6：静默上传与共享市场数据基座

### 做什么

- 实现本地 Outbox、批量幂等上报、断网恢复、上传游标、媒体对象上传和云端规范化。
- 云端按商品 ID、卖家 ID、版本指纹和上传幂等键形成市场商品池、卖家池、快照和事件链。
- 将上传批次、失败、重试、去重和数据质量全部放入 Admin；用户侧只使用最终市场数据。

### 验收

- 断网采集后恢复网络能自动补传；多次补传和多设备上报同一商品均不会生成重复实体。
- User Web 不显示上传按钮、进度、批次或同步状态；Admin 可以审计每次上传结果。
- 商品、卖家、事件、图片和历史版本可以从市场库正确关联查询。

## 阶段 7：市场洞察、AI 与运营后台

### 做什么

- 完成 Dashboard 的任务健康、竞品变化、价格趋势、异常事件和市场发现。
- 在 Admin 配置 AI 提供方、模型、密钥、提示词版本、阈值、并发、预算和发布状态。
- 云端异步执行同款聚类、竞品分析、价格带、上新频率、异常波动和机会识别；User Web 只调用已发布能力并展示结果。
- 完成 Admin 的用户、设备、套餐、订单、用量、数据质量、上传批次、队列、AI 任务和审计管理。

### 验收

- 两个采集设备上报相同商品后，市场池、事件和 Dashboard 去重正确。
- Admin 发布新的 AI 模板后，用户调用得到带版本号的结构化结果；客户端/启动器没有模型 Key 和 AI 配置入口。
- AI 失败可重试，不重复记账或重复写入结论；Admin 可以按用户、任务和时间追溯成本与结果。

## 阶段 8：容量、可靠性、打包与全链路验收

### 做什么

- 依照阶段 0 容量合同压测数据库、搜索、API、队列、对象存储和 AI Worker。
- 验证深分页、并发筛选、翻页期间新增数据、队列积压、Worker 重启、断网补传、重复上报、备份恢复和权限变更。
- 分别打包采集启动器、构建 User Web、构建 Admin Web；后续 APK 只复用 User API，不与 Admin 或采集启动器绑定。

### 验收

- 单人、多用户、多设备并发下均满足阶段 0 的查询与采集 SLO。
- 深分页无重复、无漏项，动态日志和事件在并发写入时保持稳定顺序。
- 三个产品分别部署、分别回滚、分别验证；完整链路从用户登录、设备绑定、闲鱼采集、静默上传、市场展示、AI 调用到 Admin 审计全部通过。
