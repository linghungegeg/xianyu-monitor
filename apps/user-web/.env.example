# demo keeps the workbench preview local and makes no network request.
VITE_USER_WEB_MODE=demo

# Set VITE_USER_WEB_MODE=api for the independent User API only.
VITE_USER_API_BASE_URL=http://127.0.0.1:3101
