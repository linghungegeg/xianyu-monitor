# Local preview: use fixtures and never issue an Admin API request.
VITE_ADMIN_DEMO_MODE=true

# Used only when VITE_ADMIN_DEMO_MODE=false. This is a public API origin, not a secret.
VITE_ADMIN_API_BASE_URL=http://127.0.0.1:3102
